server:
  port: 8085

spring:
  application:
    name: Config-Server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/DigvijayBakare/Hotel-Rating-System-Config-Files.git
          clone-on-start: true

