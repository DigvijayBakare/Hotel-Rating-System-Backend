spring:
  application:
    name: Hotel-Service
  datasource:
    url: jdbc:postgresql://localhost:5432/HotelRatingSystem?currentSchema=hotelservice
    username: postgres
    password: 19111011
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  config:
    import: optional:configserver:http://localhost:8085

server:
  port: 8082

#eureka:
#  instance:
#    prefer-ip-address: true
#  client:
#    fetch-registry: true
#    register-with-eureka: true
#    service-url:
#      defaultZone: http://localhost:8761/eureka

## OKTA Configuration
okta:
  oauth2:
    issuer: https://integrator-8069025.okta.com/oauth2/default
    audience: api://default